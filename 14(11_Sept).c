#include<stdio.h>
void swap(int *a, int *b) {
    int c;
    c = *a;
    *a = *b;
    *b = c;
}
int main() {
    // int x;
    // x = 5;
    // int *j;
    // j = &x;
    // printf("The value is %d\n", x);
    // printf("The address is %u\n", &x);
    // printf("The address is %u\n", j);


    // int x = 5;
    // &x = 10;

    // int x = 5;
    // int *j = &x;
    // printf("%u", j);

    // float b = 50.05651;
    // float *j = &b;


    // char ch = 'A';
    // char *p = &ch;

    // int x = 5;
    // int *ptr;
    // ptr = &x;
    // printf("The value of x is %d\n", x);
    // printf("The address of x is %u\n", &x);
    // printf("The address of x is %u\n", ptr);
    // printf("The address of ptr is %u\n", &ptr);
    // printf("This is  %d", *ptr);

    // int x = 5;
    // int *p = &x;
    // int **q = &p;
    // **q = 10;
    // **q = *p = x ;
    // printf("The value of x is %d\n", x);
    // printf("The value of x is %d\n", *p);
    // printf("The value of x is %d\n", **q);

    // int x=10,y=20;
    // swap(&x,&y);
    // printf("%d %d", x, y);

    // float b =20.61616;
    // int a = (int)b;
    // printf("%d", a);

    // int *ptr;
    // ptr = (int *)malloc(sizeof(int));
    // .
    // .
    // .
    // ..
    // .
    // .
    // ..
    // .
    // ..
    // free(ptr);
    
    return 0;
}

// %d ->  -32768 to 32768
// &x -> This is a constant thing. Hence you cant change the vlaue of address  

// int **********n = 